{
  "name": "@monorepo/platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "npm --workspace ../../shared run build && npm --workspace ../../sneakers-project run build && npm --workspace ../../shorts-project run build && vite build",
    "preview": "vite preview"

  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "vite-plugin-federation": "^0.0.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
